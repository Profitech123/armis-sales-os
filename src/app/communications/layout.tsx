import type { Metadata } from "next";
export const metadata: Metadata = { title: "Communications", description: "Sales communications requiring replies, escalation, or follow-up." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
