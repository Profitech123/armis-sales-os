import type { Metadata } from "next";
export const metadata: Metadata = { title: "Connectors", description: "Integration configuration, health, and recent activity." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
