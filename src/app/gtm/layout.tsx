import type { Metadata } from "next";

export const metadata: Metadata = { title: "GTM lead discovery", description: "Approval-gated lead-search briefs and candidate review." };

export default function GtmLayout({ children }: { children: React.ReactNode }) { return children; }

