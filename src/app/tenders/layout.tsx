import type { Metadata } from "next";
export const metadata: Metadata = { title: "Tenders", description: "Tender requirements, deadlines, and submission readiness." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
